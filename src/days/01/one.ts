import { fetchInputByDay } from "@/utils";

function solve(input: string): number {
  const lines = input.split("\n");
  return 0;
}

const rawInput = await fetchInputByDay(1);
const result = solve(rawInput);

console.log(result);
